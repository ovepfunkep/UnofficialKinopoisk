<Page
    x:Class="UWPUnofficialKinopoisk.FilmsCollectionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWPUnofficialKinopoisk"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:UWPUnofficialKinopoisk.Controls"
    mc:Ignorable="d"
    Name="Kinosearch">
    <Grid>
        <ScrollViewer x:Name="svFilms" Background="#FF171717">
            <ListBox ItemsSource="{Binding FilmsList}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <controls:FilmControl DataContext="{Binding}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>
    </Grid>
</Page>
